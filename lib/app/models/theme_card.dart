import 'package:flutter/material.dart';

/// Model for the theme of a business card.
class ThemeCard {
  Color background;
  Color foreground;

  ThemeCard({required this.background, required this.foreground});
}
